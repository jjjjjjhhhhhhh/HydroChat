; Pytest configuration to ensure backend modules import correctly
[pytest]
pythonpath = backend
testpaths = backend/apps/hydrochat/tests
addopts = -q --ds=config.settings
DJANGO_SETTINGS_MODULE = config.settings
