# Environment Variables Template
# Copy this file to .env and fill in your actual values

# API Base IP Address
# Update this IP address to match your computer's local network IP
# You can find your IP address by running the backend server (it will display available IPs)
# or by using 'ipconfig' on Windows or 'ifconfig' on Mac/Linux

# For mobile testing, use your computer's local network IP address
API_BASE_URL=192.168.1.100

# For localhost testing (web only)
# API_BASE_URL=127.0.0.1

# Example IP addresses you might see:
# API_BASE_URL=192.168.1.100
# API_BASE_URL=172.30.1.54
# API_BASE_URL=10.0.1.100 